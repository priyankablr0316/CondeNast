package com.assessment.condenast.ui

import android.app.Application
import com.assessment.condenast.ui.base.BaseViewModel
import javax.inject.Inject

/**
 * Unused for now but can be used in MainActivity in case needed.
 */
class MainViewModel @Inject constructor(application: Application) : BaseViewModel(application)